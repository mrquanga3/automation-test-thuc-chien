# Copy to .env and adjust. Do not commit .env with real secrets.
BASE_URL=http://103.245.237.118:8081
ADMIN_USER=admin
ADMIN_PASSWORD=admin
# HEADLESS=1

# Android / Appium (npm run test:android) and cross web+mobile (npm run test:cross)
# Serial from `adb devices` — first emulator is usually emulator-5554
ANDROID_UDID=emulator-5554
# Google Messages defaults; for AOSP try com.android.messaging + its launcher activity
# MESSAGES_APP_PACKAGE=com.google.android.apps.messaging
# MESSAGES_APP_ACTIVITY=com.google.android.apps.messaging.ui.ConversationListActivity
